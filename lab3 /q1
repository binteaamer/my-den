//ans: the input was out of int range
#include<stdio.h>
int main(void) {
    // Declare a variable of type long long int
    long long int a = 3000000000LL;
    // Print the value of the variable
    printf("%lld\n", a);
    return 0;
}
