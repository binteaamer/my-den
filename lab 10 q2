#include<stdio.h>

#include<math.h> 
typedef struct (
int x; int y;

point;

int distance(point p1, point p2);
int main()(

point pl;

scanf("%d" "%d", &p1.x, &p1.y);

point p2;
scanf("%d" "%d", &p2.x, &p2.y);

distance(p1, p2);


int distance(point p1, point p2)

int dis-sqrt(pow((p2.x)-(p1.x),2)+pow((p2.y)-(p1.y),2)); printf("\ndistance bw two points: %d", dis);

